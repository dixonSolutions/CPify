# CPify Build Requirements
# This is a C/GTK4 project built with Meson.
# These are the required system packages and libraries.

# Build Tools
meson
ninja-build
gcc
pkg-config

# Core Libraries (Development Headers Required)
gtk4 >= 4.10
libadwaita-1 >= 1.4
glib-2.0
gio-2.0

# Media/GStreamer
gstreamer-1.0 >= 1.18
gstreamer-video-1.0
gstreamer-audio-1.0
gstreamer-plugins-base
gstreamer-plugins-good
gstreamer-plugins-bad
gstreamer-libav

# Networking (Auto-Updater)
libsoup-3.0 >= 3.0
json-glib-1.0 >= 1.6
